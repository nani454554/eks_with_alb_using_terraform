# eks_with_alb_using_terraform

### tested using terraform code is running, alb controller, autoscaler, eks, iam, ingress, vpc  and nlb is remove because i'm using alb controller, it will automatically route traffic.
